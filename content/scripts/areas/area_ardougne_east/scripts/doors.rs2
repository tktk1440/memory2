[oploc1,elenadoor2]
switch_int(%elenaquest) {
    case ^elena_complete, ^elena_complete_read_scroll : ~open_and_close_door2(loc_param(next_loc_stage), ~check_axis(coord, loc_coord, loc_angle), door_open);
    case default : mes("The door is locked.");
}
